/// <reference path="../app/app.d.ts" />

module kdc.constants {
    
     export const app = 'kdc';
     export const core = app + '.core';
     export const layout = app + '.layout';
     export const blocks = app + '.blocks';
     export const services = app + '.services';
     export const logService = services + '.log';
               
     export const about = app + '.about';
     export const bookmarks = app + '.bookmarks';
     export const intro = app + '.intro';
     export const posts = app + '.posts';
     export const settings = app + '.settings';
     export const tabs = app + '.tabs';
     export const filters = app + '.filters';
     
     export const removeShareFilter = app + '.filters.removeShare';
     export const externalLinkFilter = app + '.filters.externalLink';
     
     export const baseUrl = 'https://www.kingdavidconsulting.com/'; //'http://demo.wp-api.org/'
         
}